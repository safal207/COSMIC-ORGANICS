[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cosmic-organics-morphos"
version = "0.1.0"
description = "MORPHOS-0 deterministic state-transition reference simulator"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}

[tool.setuptools]
packages = ["morphos"]
